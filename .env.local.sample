# Domain settings for instance
# Set the domain setting for your host
DOMAIN=yourdomain.com
NEXT_PUBLIC_DOMAIN=yourdomain.com

# Screenshot API Token screenshotapi.net
SCREENSHOTAPI_TOKEN=[API TOKEN]

# NEXT_PUBLIC_GIF_PATH=/images/arrow.gif
# SSL Certificate setup
# Try to setup legit certs with ( e.g. certbot )
SSL_KEY_PATH="/path/to/privkey.pem"
SSL_CERT_PATH="/path/to/fullchain.pem"

# BYPASS MODE SETTINGS ( testing without pumping in accounts )
BYPASS_MODE=OFF
VENDASTA_BUSINESS_ID=[AGID of account for BYPASS mode]
BYPASS_WEBHOOK=http://automations.businessapp.io/start/PID/automation_id
MY_LISTING_DEFAULT="https://sales.vendasta.com/vendasta-mlkvmcg2/"

# PostgreSQL stuff (Docker setup)
DB_USER=postgres
DB_HOST=localhost
DB_PASSWORD=botpress_password
DB=mp
DB_PORT=5433

# Botpress Configuration (Botpress Cloud)
BOTPRESS_SERVER_URL=https://chat.botpress.cloud
BOTPRESS_BOT_ID=[your-bot-id-here]
BOTPRESS_CLIENT_ID=[your-workspace-id-here]
BOTPRESS_WEBHOOK_SECRET=[random-string-for-webhook-security]
BOTPRESS_API_TOKEN=[your-personal-access-token-here]
JWT_SECRET=[random-secret-for-jwt-signing]

# Public Botpress variables (exposed to browser)
NEXT_PUBLIC_BOTPRESS_BOT_ID=[your-bot-id-here]
NEXT_PUBLIC_BOTPRESS_CLIENT_ID=[your-workspace-id-here]

# Vendasta variables
ENV=prod
WEBHOOK=http://automations.businessapp.io/start/PID/automation_id
VENDASTA_PARTNER_ID=PID
VENDASTA_PRIVATE_KEY_ID=[service account ID]
VENDASTA_CLIENT_EMAIL=[email used to create service account]
VENDASTA_TOKEN_URI=https://sso-api-prod.apigateway.co/oauth2/token
VENDASTA_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----The\nOle\nPrivate\nKey-----END RSA PRIVATE KEY-----\n

# API Key info for PDL and other enrich calls - using to look up company name
NEXT_PUBLIC_PDL_API_KEY=[API Key from PDL]
NEXT_PUBLIC_PDL_API_URL=https://api.peopledatalabs.com/v5/company/enrich
# Not using right now Hunter_API_Key=xxxxx

# Zapier URL (DEPRECATED - now using Node.js scrapers)
# NEXT_PUBLIC_ZAP_URL=https://hooks.zapier.com/hooks/catch/15076287/3vv4bs9/

# OpenAI Settings for intelligent snippet extraction from scraped content
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
# Model to use for extraction (gpt-4o-mini is recommended for cost efficiency)
OPENAI_MODEL=gpt-4o-mini
# Enable/disable OpenAI extraction (set to false to use basic extraction)
USE_OPENAI_EXTRACTION=true

# Snippet Display Settings
# Number of snippets to display before transitioning to chatbot (max 10)
SNIPPET_SHOW=5

# Email Settings (Resend)
# Sign up at https://resend.com and get your API key
# You'll need to verify your domain (membies.com) in Resend dashboard
RESEND_API_KEY=re_xxxxxxxxxx
EMAIL_FROM="Magic Page <noreply@membies.com>"
# Email address to receive signup notifications
NOTIFY_EMAIL=you@yourdomain.com
# Base URL for shareable links
SHARE_LINK_BASE_URL=https://mb.membies.com
